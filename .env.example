# Database Configuration
# Copy this file to .env and fill in your actual database credentials

# Database Connection Details
DB_HOST=localhost
DB_PORT=3306
DB_NAME=eventman_db
DB_USERNAME=root
DB_PASSWORD=your_actual_mysql_password

# Optional: Server Port (default is 8080)
# SERVER_PORT=8080

# Optional: JWT Secret (generate a secure random string)
# JWT_SECRET=your_jwt_secret_key_here

# Note: Set DB_PASSWORD to your actual MySQL root password
# The application will use these values to connect to your database
